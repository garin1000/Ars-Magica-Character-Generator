{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core access for the main window. Custom commands are always invokable; the dialog plugin is driven from Rust, so only its default set is granted.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
